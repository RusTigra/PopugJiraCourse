@model TaskBoard.Models.Output.PopugTaskView
@using TaskStatus = TaskBoard.Infrastructure.TaskStatus

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit</title>
</head>
<body>

    <h4>PopugTask</h4>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Id" />
                <div class="form-group">
                    <label>@Html.LabelFor(m => m.Content)</label>
                    @Html.Raw(Model.Content)
                </div>
                @*<div class="form-group">
                        <label asp-for="Responsible" class="control-label"></label>
                        <input asp-for="Responsible" class="form-control" />
                        <span asp-validation-for="Responsible" class="text-danger"></span>
                    </div>*@
                <div class="form-group">
                    <label>@Html.LabelFor(m => m.Status)</label>
                    @Html.DropDownListFor(m => m.Status, new SelectList(
                        new List<TaskStatus?> { null, TaskStatus.New, TaskStatus.Taken, TaskStatus.Closed }))
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>

    <div>
        <a asp-action="Index">Back to List</a>
    </div>

</body>
</html>
